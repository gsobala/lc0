version: '{build}'
configuration: Release
platform: x64
image: macos
install:
- python3 -m venv venv
- source ./venv/bin/activate
- pip3 install meson
- pip3 install ninja
- export PATH=/Users/appveyor/Library/Python/3.7/bin:$PATH
build_script:
- meson build/release --buildtype release 
- cd build/release
- ninja
artifacts:
- path: build/release/lc0
  name: lc0
test_script:
- ./encoder_test

